# Changelog

所有重大变更都将记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [5.2.2] - 2026-08-29

### 修复
- 🖼 **修复打包版 EXE 窗口标题栏/任务栏图标仍为默认羽毛的问题** - 此前 icon.ico 未打包进 EXE，运行时找不到文件静默跳过；现图标随 EXE 打包并在运行时从自解压目录加载（源码运行不受影响）
- 🔖 同步界面版本号显示（v5.2.1 曾遗漏，界面仍显示 v5.2.0）

---

## [5.2.1] - 2026-08-28

### 修复
- 🐛 **修复部分环境下 EXE 双击无法启动的问题**（报 "Can't find a usable init.tcl"）- 新版 PyInstaller 在 TEMP 环境变量指向非常规位置时可能改用无权限的系统临时目录导致解压失败；现改为运行时解压到 EXE 所在目录（`--runtime-tmpdir .`），不再依赖任何 TEMP 环境变量，任意环境双击即可运行（正常退出时自动清理）
- 🔒 CI 构建锁定 `pyinstaller==6.17.0`，避免依赖漂移导致打包行为变化

---

## [5.2.0] - 2026-08-28

### 新增
- 📌 **窗口置顶按钮** - 底部栏新增 📌 小按钮，点击后窗口始终浮在其他程序上层（按钮高亮为强调色表示置顶中），从资源管理器拖拽文件校验时无需先切换窗口
- 🖼 **自定义窗口图标** - 源码运行时窗口左上角显示应用自有图标（此前为 Tk 默认羽毛图标，仅打包版有）

---

## [5.1.1] - 2026-08-14

### 修复
- 🐛 修复 2K 显示器 200% 缩放等高分屏下窗口偏小、界面被裁切的问题 - 开启 DPI 感知后，窗口尺寸与内边距等像素值按 DPI 等比缩放，与字体缩放保持一致

---

## [5.1.0] - 2026-08-12

### 优化
- 🖥️ **适配 Windows 高 DPI 显示** - 在 2K 分辨率、200% 系统缩放等场景下启用 Per-Monitor V2 DPI 感知，减少 Tkinter 界面被系统位图放大造成的模糊

---

## [5.0.0] - 2026-06-24

### 变更
- 🔧 **JSON 校验改为严格 JSONC 模式** - JSON 文件现按 JSONC（JSON with Comments）规范校验，替代此前宽松的 JSON5 解析
  - 注释（`//`、`/* */`）依旧支持忽略
  - 现会正确拒绝 JSON5 私有语法：单引号字符串、未加引号的键、十六进制数字、前导/尾随小数点等（此前被误判为"解析正常"）
  - 尾随逗号仍按公司规范视为错误
- 📦 **移除依赖** `json5`，改用 Python 标准库 `json` 进行严格解析
- 🚫 **移除 JSON5 支持** - 不再接受 `.json5` 扩展名，仅支持 `.json`（普通 JSON 与 JSONC）

### 优化
- 🎯 **JSON 错误定位更精准** - 以标准库 `json` 作为权威解析器，解析失败时附带精确的「行:列」位置并翻译为中文（如"键名缺少双引号""缺少逗号"等），方便快速排查
  - 修复"结构预检"因字符串内部出现的括号而误报的问题（`json` 能正确忽略字符串内容）
  - 括号不平衡等结构问题降级为「💡 参考分析」辅助显示，不再遮蔽真正的第一处错误
- 🧭 **缺括号位置智能定位** - 当 JSON 少了闭合 `}`/`]` 时，"参考分析"改用「缩进不匹配 + 跨度最大的叶子」启发式，精准指向最可能缺闭合的对象（不再像旧版那样盲目猜测一个跑偏的嫌疑位置）

### 修复
- 🐛 修复 JSON5 解析器过于宽松、将 JSONC 非法语法误判为"解析正常"的问题

---

## [4.1.0] - 2026-04-21

### 新增
- 🔍 **不可见特殊字符检测** - 检测文件中隐藏的不可见字符，覆盖 15 种字符类型：
  - NO-BREAK SPACE (`\xa0`)、ZERO WIDTH SPACE (`\u200b`)、BOM (`\ufeff`)、全角空格 (`\u3000`) 等
  - JSON 和 YAML 文件均会检测
  - 输出精确的行列位置和上下文提示

### 修复
- 🐛 修复 JSON 文件中不可见字符导致的漏检问题（关闭 #2）

---

## [4.0.2] - 2026-02-05

### 修复
- 🐛 彻底修复 PyInstaller 打包后 yamllint 找不到 `default.yaml` 的问题
  - 移除配置中的 `extends: default`，直接内嵌完整规则
  - 同步更新 `.yamllint` 配置文件

---

## [4.0.1] - 2026-02-02

### 修复
- 🐛 修复 PyInstaller 打包后 yamllint 找不到默认配置文件的问题

---

## [4.0.0] - 2026-01-30

### 重构
- 🔧 **YAML 检测底层重构** - 使用专业工具 [yamllint](https://github.com/adrienverge/yamllint) 替代手动实现
- 📦 **新增依赖** - `yamllint`（YAML 校验核心引擎）

### 新增
- 📁 **文件夹批量检测** - GUI/CLI 均支持拖入文件夹，自动递归扫描 `.json/.json5/.yaml/.yml` 文件
- 📊 **检测汇总报告** - 批量检测时显示正常/警告/错误文件分类统计
- 📝 **日志保存功能** - 可选保存检测结果到日志文件（含时间戳）
- ❌/⚠️ **错误分级显示** - 区分 error（必须修复）和 warning（建议修复）两个级别
- 🎨 **颜色分级** - 全部正常显示绿色，仅有警告显示橙色，存在错误显示红色
- 🌐 **中文友好提示** - 将 yamllint 英文报错翻译为中文，格式 `行:列`
- 📋 **内嵌配置** - yamllint 配置内嵌代码，无需外部 `.yamllint` 文件
- 🔍 **更多检测规则** - 冒号空格、大括号空格、布尔值歧义、行长度等

### 优化
- ✨ **代码精简** - 删除 200+ 行手动检测代码，使用专业库更可靠
- 🖼️ **文件分隔优化** - 批量检测时每个文件使用醒目的分隔线区分
- 📋 **复制MD5按钮状态** - 多文件检测时自动禁用，避免误操作

### 鸣谢
- 本版本 YAML 检测功能完全基于 [yamllint](https://github.com/adrienverge/yamllint) 项目实现，感谢 Adrien Vergé 及所有贡献者

---

## [3.1.0] - 2026-01-27

### 新增
- 🔍 **保留字符检测** - 检测 `@` 和 `` ` `` 开头的非法纯标量值
- 📋 **多错误收集** - 一次性报告所有发现的 YAML 问题，不再只显示第一个错误
- 💡 **修正建议** - 重复键错误现在会提供数组语法的修正示例

### 优化
- 📝 **缩进错误提示** - 优化提示信息，更准确地说明"缩进减少时必须回退到之前使用过的层级"
- 📚 **README 文档** - 添加完整的 YAML 关键错误检测能力表格（基于 YAML 1.2.2 规范）

---


## [3.0.3] - 2026-01-23


### 新增
- ✨ 版本号点击可跳转 GitHub 仓库
- 🔗 版本号样式优化，高亮显示

---

## [3.0.2] - 2026-01-22

### 修复
- 🐛 修复 GitHub Actions 编译时图标路径配置错误

---

## [3.0.1] - 2026-01-21

### 修复
- 🐛 修复应用图标边缘白边问题，优化透明圆角效果
- 📝 更新文档中的版本号

---

## [3.0.0] - 2026-01-20

### 新增
- 🔐 **MD5 计算** - 显示文件 MD5 值，一键复制，支持任意文件格式
- 🎨 **全新设计** - 现代极客风深色主题 UI，配套新版应用图标

### 变更
- 📦 GitHub Actions 自动编译现已支持图标
- 📝 更新 README 添加编译说明

---

## [2.0.0] - 2026-01-20

### 新增
- ✨ **YAML 文件支持** - 完整的 YAML/YML 文件校验功能
- 🔍 **缩进一致性检测** - 检测空格与Tab混用问题，报告具体行号
- 🔍 **缩进层级检测** - 识别不符合YAML缩进规则的行
- 🔍 **重复键检测** - 发现同一层级重复定义的键名（智能排除列表项）
- 🤖 **自动格式识别** - 根据文件扩展名自动选择 JSON 或 YAML 解析器

### 变更
- 📦 新增依赖 `pyyaml`
- 🎨 GUI 拖拽区提示更新为 "拖拽 JSON/YAML 文件至此处校验"
- 📝 更新 README 文档

---

## [1.0.0] - 2026-01-16

### 新增
- 🎉 首个正式版本发布
- ✨ **JSON/JSON5 校验** - 支持标准 JSON 和 JSON5 格式
- 🔍 **智能注释识别** - 自动忽略 `//` 和 `/* */` 注释
- 🔍 **深度结构分析** - 追踪 `{}`、`[]`、`""` 层级，识别不匹配括号
- 🔍 **缺失逗号检测** - 发现遗漏逗号的行
- 🔍 **尾随逗号检测** - 检测异常的尾随逗号
- 💻 **命令行模式** - 支持批量校验多个文件
- 🖥️ **GUI 模式** - 拖拽文件校验，支持 Windows 桌面使用

---

## 版本说明

- **主版本号 (Major)**: 不兼容的 API 或功能变更
- **次版本号 (Minor)**: 向后兼容的功能新增
- **修订号 (Patch)**: 向后兼容的问题修复
